---
import Header from "~/components/header.astro";
import Footer from "~/components/footer.astro";
import MetaHead from "~/components/meta-head.astro";
import Starfield from "~/components/starfield.astro";
import star from "~/assets/star.png";
import "@fontsource-variable/inter";
import "~/styles/index.css";

const title = "EtchNFT — About";
const description = "Why we’re here. What we believe. What we’re building.";
---

<html lang="en" class="h-full motion-safe:scroll-smooth" data-theme="dark">
  <MetaHead {title} {description} />

  <body class="bg-black text-white font-sans selection:bg-pink-600 selection:text-white overflow-x-hidden relative">
    <Header />
    <Starfield />
     <img src={star.src} alt="Star" class="absolute top-4 right-4 w-8 h-8 object-contain animate-float opacity-0 animate-fade-in" />
    <main class="relative z-20 px-6 pt-32 pb-48 max-w-6xl mx-auto text-center">
      <!-- Headline -->
      <h1 class="text-4xl md:text-6xl font-bold tracking-tight text-[#DB2777] mb-6">
        Why EtchNFT Exists
      </h1>
      <p class="text-lg text-gray-300 max-w-2xl mx-auto mb-12">
        We started EtchNFT to capture something real. A moment. A message. A memory — 
        <span class="text-[#41C6BB]">minted permanently</span> and wrapped in culture you can actually hold.
      </p>

      <!-- Ethos -->
      <section class="grid grid-cols-1 md:grid-cols-3 gap-12 text-left mb-24">
        <div>
          <h3 class="text-[#DB2777] font-semibold mb-2">🧱 On-Chain > Cloud</h3>
          <p class="text-sm text-gray-400">
            When it matters, it should be permanent. Our inscriptions live on public ledgers — not private servers.
          </p>
        </div>
        <div>
          <h3 class="text-[#41C6BB] font-semibold mb-2">🛠 Craft > Mass</h3>
          <p class="text-sm text-gray-400">
            Each drop is handcrafted. Pressed. Etched. One at a time.
          </p>
        </div>
        <div>
          <h3 class="text-[#7F73F7] font-semibold mb-2">🔮 Expression > Speculation</h3>
          <p class="text-sm text-gray-400">
            This isn’t about flipping. It’s about making artifacts that actually mean something.
          </p>
        </div>
      </section>

      <!-- Community Quote -->
      <blockquote class="italic text-pink-500 text-lg mb-20">
        “We’re not just building a product. We’re building a ritual.”
        <footer class="text-white/50 mt-2">— EtchNFT</footer>
      </blockquote>

      <!-- Supporter Tiers -->
      <section class="grid grid-cols-1 md:grid-cols-3 gap-10 text-left text-sm mb-24">
        <div class="bg-white/5 border border-white/10 rounded-xl p-6 shadow-md">
          <h4 class="text-[#DB2777] font-semibold mb-2">👑 OG Collectors</h4>
          <p class="text-gray-400">First 100 wallets to back the project — Special edition drop #0.</p>
        </div>
        <div class="bg-white/5 border border-white/10 rounded-xl p-6 shadow-md">
          <h4 class="text-[#7F73F7] font-semibold mb-2">🦄 Founding Members</h4>
          <p class="text-gray-400">Early adopters who helped us test, share, and spread the mint gospel.</p>
        </div>
        <div class="bg-white/5 border border-white/10 rounded-xl p-6 shadow-md">
          <h4 class="text-[#41C6BB] font-semibold mb-2">🪩 Backers & Builders</h4>
          <p class="text-gray-400">Anyone who’s supported us — through ideas, feedback, or ETH.</p>
        </div>
      </section>

      <!-- Highlight Bar -->
      <section class="bg-gradient-to-r from-[#DB2777]/20 via-[#7F73F7]/20 to-[#41C6BB]/20 rounded-xl px-6 py-8 mb-20">
        <h2 class="text-xl font-semibold text-white mb-2">Etch is just getting started.</h2>
        <p class="text-sm text-gray-400 max-w-2xl mx-auto">
          This is more than a drop. It's a foundation — for permanent memory artifacts, for on-chain identity,
          for collectibles that mean something beyond speculation.
        </p>
      </section>

      <!-- CTA Button -->
      <div class="text-center">
        <a
          href="/waitlist"
          class="inline-block bg-pink-600 hover:bg-pink-700 text-white font-bold py-3 px-6 rounded-lg transition shadow-lg"
        >
          Join the Waitlist
        </a>
      </div>
    </main>

    <Footer />

    <style>
      @keyframes float {
        0% { transform: translateY(0px) rotate(0deg); opacity: 0.15; }
        50% { transform: translateY(-12px) rotate(3deg); opacity: 0.3; }
        100% { transform: translateY(0px) rotate(0deg); opacity: 0.15; }
      }

      .animate-float {
        animation: float 6s ease-in-out infinite;
        filter: drop-shadow(0 0 14px rgba(127, 115, 247, 0.35));
      }
    </style>
  </body>
</html>

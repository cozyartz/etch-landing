{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",              // Required for `paths` to work
    "paths": {
      "~/*": ["src/*"]           // Allow imports like ~/components/Header
    },
    "types": ["astro/client"]     // Ensures Astro types are included
  }
}
